# Claude Bridge Server v1.2 Dependencies
# ========================================
# Install with: pip install -r requirements.txt
# or with uv: uv pip install -r requirements.txt

PyYAML>=6.0
beautifulsoup4>=4.12.0
Pillow>=10.0.0
